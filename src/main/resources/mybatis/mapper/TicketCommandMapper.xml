<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zwn.trainserverspringboot.command.mapper.TicketCommandMapper">

    <insert id="ticketBooking">
        INSERT INTO ticket_order(user_id,passenger_id,departure_date,train_route_id,order_status,order_time,price)
        VALUES (#{param1},#{param4},#{param2},#{param3},#{param5},#{param6},#{param7})
    </insert>
</mapper>